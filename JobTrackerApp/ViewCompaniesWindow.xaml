<Window x:Class="JobTrackerApp.ViewCompaniesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="View Companies - Job Application Tracker" Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
       WindowState="Maximized"
        Background="#0a0e27">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6366f1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4f46e5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1e293b"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Padding="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#1e293b"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Style for ComboBox Items (Dropdown Items) -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1e293b"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4f46e5"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#6366f1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#1e293b"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowBackground" Value="#1e293b"/>
            <Setter Property="AlternatingRowBackground" Value="#0f172a"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="45"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0f172a"/>
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="Height" Value="50"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#1e293b"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#334155"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#475569"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="15,0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <StackPanel HorizontalAlignment="Left">
                <TextBlock Text="All Companies" 
                           FontSize="32" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="View, search, filter and manage your job applications" 
                           FontSize="14" Foreground="#94a3b8" Margin="0,5,0,0"/>
            </StackPanel>

            <Button Content="➕ Add New Company" 
                    Style="{StaticResource ModernButtonStyle}"
                    Click="BtnAddNew_Click"
                    Background="#10b981"
                    Padding="25,12"
                    FontSize="15"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Filter Section -->
        <Border Grid.Row="1" Background="#1e293b" CornerRadius="12" Padding="25" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Text="🔍 Search" Foreground="#94a3b8" Margin="0,0,0,8" FontWeight="SemiBold"/>
                    <TextBox x:Name="txtSearch" Style="{StaticResource ModernTextBoxStyle}" 
                             TextChanged="TxtSearch_TextChanged"
                             ToolTip="Search by company name, tech stack, location or email"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="15,0">
                    <TextBlock Text="Filter by Status" Foreground="#94a3b8" Margin="0,0,0,8" FontWeight="SemiBold"/>
                    <ComboBox x:Name="cmbFilterStatus" Style="{StaticResource ModernComboBoxStyle}"
                              SelectionChanged="CmbFilterStatus_SelectionChanged">
                        <ComboBoxItem Content="All" IsSelected="True"/>
                        <ComboBoxItem Content="Not Called"/>
                        <ComboBoxItem Content="Called"/>
                        <ComboBoxItem Content="Interview Scheduled"/>
                        <ComboBoxItem Content="Rejected"/>
                        <ComboBoxItem Content="Offer Received"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="15,0">
                    <TextBlock Text="Sort by" Foreground="#94a3b8" Margin="0,0,0,8" FontWeight="SemiBold"/>
                    <ComboBox x:Name="cmbSort" Style="{StaticResource ModernComboBoxStyle}"
                              SelectionChanged="CmbSort_SelectionChanged">
                        <ComboBoxItem Content="Date Added (Newest)" IsSelected="True"/>
                        <ComboBoxItem Content="Date Added (Oldest)"/>
                        <ComboBoxItem Content="Company Name (A-Z)"/>
                        <ComboBoxItem Content="Company Name (Z-A)"/>
                    </ComboBox>
                </StackPanel>

                <Button Grid.Column="3" Content="📤 Export CSV" 
                        Style="{StaticResource ModernButtonStyle}"
                        Click="BtnExport_Click"
                        Background="#8b5cf6"
                        Padding="20,10"
                        VerticalAlignment="Bottom"
                        Margin="15,0,0,0"/>
            </Grid>
        </Border>

        <!-- DataGrid Section -->
        <Border Grid.Row="2" Background="#1e293b" CornerRadius="12" Padding="0">
            <DataGrid x:Name="dgCompanies" 
                      Style="{StaticResource ModernDataGridStyle}"
                      SelectionChanged="DgCompanies_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName}" Width="180"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="130"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="220"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="150"/>
                    <DataGridTextColumn Header="Tech Stack" Binding="{Binding TechStack}" Width="200"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="150"/>
                    <DataGridTextColumn Header="Date Added" Binding="{Binding DateAdded, StringFormat='dd/MM/yyyy'}" Width="110"/>
                    <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}" Width="*" MinWidth="200"/>
                    <DataGridTemplateColumn Header="Actions" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️ Edit" 
                                            Background="#3b82f6" 
                                            Foreground="White"
                                            BorderThickness="0"
                                            Padding="12,6"
                                            Cursor="Hand"
                                            Click="BtnEdit_Click"
                                            Margin="0,0,5,0"
                                            FontSize="12"/>
                                    <Button Content="🗑️ Delete" 
                                            Background="#ef4444" 
                                            Foreground="White"
                                            BorderThickness="0"
                                            Padding="12,6"
                                            Cursor="Hand"
                                            Click="BtnDelete_Click"
                                            FontSize="12"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>